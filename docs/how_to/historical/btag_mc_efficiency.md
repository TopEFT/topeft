# Legacy b-tag MC efficiency workflow

> **Archival, unsupported interface.** The source remains under
> `analysis/btagMCeff`, but it has not been updated since the August 2023
> `topcoffea` refactoring. Do not use it to produce a current TOP-26-006
> correction payload without a separate source review and implementation
> decision.

This note preserves the purpose of the directory: `btagMCeff.py`, `run.py`, and
`RunBtag.sh` supported an older b-tagging MC-efficiency derivation. The current
source does not establish a maintained command, environment, payload schema, or
installation path. Import fixes alone would not demonstrate physics or
software compatibility.
